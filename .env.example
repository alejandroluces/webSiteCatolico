# Variables de entorno para el proyecto Luz de Fe

# Configuración de Supabase para el cliente (Vite)
# Estas son seguras para exponer en el frontend
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_anon_key

# Configuración de Supabase para el backend (Scripts, Funciones)
# ¡NO COMPARTIR ESTA CLAVE!
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# --- WhatsApp (Green API) ---
# Necesario para enviar el Evangelio por WhatsApp desde las Netlify Functions.
# GREEN_API_API_URL (opcional): URL regional indicada en el panel de Green API.
# Ejemplo: https://7105.api.greenapi.com
GREEN_API_API_URL=https://7105.api.greenapi.com
GREEN_API_ID_INSTANCE=your_green_api_id_instance
GREEN_API_TOKEN=your_green_api_token

# --- IA para reflexiones (opcional) ---
# Proveedor para generar la reflexión del evangelio:
#   auto     -> intenta en orden: Gemini -> OpenAI -> DeepSeek
#   gemini   -> solo Gemini
#   openai   -> solo OpenAI/ChatGPT
#   deepseek -> solo DeepSeek
#   none     -> NO usa IA, usa una reflexión de respaldo (texto fijo)
REFLECTION_PROVIDER=auto

# --- Gemini (opcional si REFLECTION_PROVIDER no usa gemini) ---
GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-2.0-flash-001

# --- OpenAI / ChatGPT ---
# Se usa SIEMPRE para audios TTS en scripts/updateDailyGospel.js
OPENAI_API_KEY=your_openai_api_key
# Modelo para generar reflexión si REFLECTION_PROVIDER=openai o auto
OPENAI_REFLECTION_MODEL=gpt-4o-mini

# --- DeepSeek (opcional) ---
DEEPSEEK_API_KEY=your_deepseek_api_key
# Base URL compatible OpenAI
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat

# Email para notificaciones del sistema
NOTIFICATION_EMAIL=admin@luzdefe.com
