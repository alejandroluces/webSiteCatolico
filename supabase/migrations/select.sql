-- Consulta para ver todo el contenido, ordenado por el más reciente primero.
-- Esto es útil para ver rápidamente las últimas entradas que has añadido.
SELECT * FROM entries
ORDER BY created_at DESC;